class PresenterState {
  final bool initialState;
  final bool getMoreEnabled;
  final bool showSelection;

  PresenterState(
   this.initialState,
    this.getMoreEnabled,
    this.showSelection,
  );
}